# Docker

* **macOS**
```
On MacOS the **docker** binary is only a client and you cannot use it to run the docker daemon.

Docker daemon uses Linux-specific kernel features, therefore you can't run Docker natively in OS X.


```
